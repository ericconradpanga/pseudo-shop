# Displays product listings

function display_products():
    product_list = get_all_products()

    for each product in product_list:
        print product.name
        print product.height + "x" + product.width + "x" + product.length
        print product.brand
        print product.color
        print newline
